import React from 'react';

type Props = {};

const Header = (props: Props) => {
  return (
    <header className="flex items-center justify-center w-full font-bold h-20 bg-orange-300">
      Header
    </header>
  );
};

export default Header;
